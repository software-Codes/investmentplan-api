feat: complete authentication system with admin user management

- User registration with email OTP verification and KYC upload
- JWT authentication with session validation and token blacklisting
- Password reset with OTP (min 8 characters)
- Profile management: update name, phone, photo (multipart form-data)
- Account deletion with password confirmation

- Admin user management: block, unblock, force logout, delete users
- Account status validation prevents suspended users from login/API access
- Audit logging for all admin actions

- Database migrations: profile_photo_url column, admin_actions table
- Supabase Storage integration for profile photos and KYC documents
- Fixed repository queries to match database schema

- API documentation consolidated into single files
- Consistent response format and error handling across all endpoints
